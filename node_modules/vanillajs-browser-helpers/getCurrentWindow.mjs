import getCurrentDocument from './getCurrentDocument';
import isWindow from './isWindow';
export default function getCurrentWindow(obj) {
    if (isWindow(obj)) {
        return obj;
    }
    const doc = getCurrentDocument(obj);
    return doc && doc.defaultView;
}
