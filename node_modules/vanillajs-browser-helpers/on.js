"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var isEventTarget_1 = __importDefault(require("./isEventTarget"));
var eventOptionsSupported_1 = __importDefault(require("./eventOptionsSupported"));
function on(elm, eventNames, handler, options) {
    if (!(0, isEventTarget_1.default)(elm)) {
        options = handler;
        handler = eventNames;
        eventNames = elm;
        elm = document;
    }
    var opts = (0, eventOptionsSupported_1.default)()
        ? options
        : !!(options && options.capture);
    if (!Array.isArray(eventNames)) {
        eventNames = [eventNames];
    }
    eventNames.forEach(function (evt) { return elm.addEventListener(evt, handler, opts); });
    return elm;
}
exports.default = on;
