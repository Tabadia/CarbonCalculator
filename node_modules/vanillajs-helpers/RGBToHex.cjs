"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const minMax_1 = __importDefault(require("./minMax"));
const numberToHex_1 = __importDefault(require("./numberToHex"));
const hexStr = (color) => (0, numberToHex_1.default)((0, minMax_1.default)(color, 0, 255));
function RGBToHex(r, g, b, a) {
    if (Array.isArray(r)) {
        [r, g, b, a] = r;
    }
    let hex = '#'
        + hexStr(r)
        + hexStr(g)
        + hexStr(b);
    if (a !== undefined && a < 1) {
        if (a < 0) {
            a = 0;
        }
        hex += hexStr(a * 255);
    }
    return hex;
}
exports.default = RGBToHex;
