"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var minMax_1 = __importDefault(require("./minMax"));
var numberToHex_1 = __importDefault(require("./numberToHex"));
var hexStr = function (color) { return (0, numberToHex_1.default)((0, minMax_1.default)(color, 0, 255)); };
function RGBToHex(r, g, b, a) {
    var _a, _b;
    if (Array.isArray(r)) {
        _a = r, _b = __read(_a, 4), r = _b[0], g = _b[1], b = _b[2], a = _b[3];
    }
    var hex = '#'
        + hexStr(r)
        + hexStr(g)
        + hexStr(b);
    if (a !== undefined && a < 1) {
        if (a < 0) {
            a = 0;
        }
        hex += hexStr(a * 255);
    }
    return hex;
}
exports.default = RGBToHex;
