import minMax from './minMax';
import hex from './numberToHex';
const hexStr = (color) => hex(minMax(color, 0, 255));
function RGBToHex(r, g, b, a) {
    if (Array.isArray(r)) {
        [r, g, b, a] = r;
    }
    let hex = '#'
        + hexStr(r)
        + hexStr(g)
        + hexStr(b);
    if (a !== undefined && a < 1) {
        if (a < 0) {
            a = 0;
        }
        hex += hexStr(a * 255);
    }
    return hex;
}
export default RGBToHex;
