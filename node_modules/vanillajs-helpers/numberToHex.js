"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
var leadingZero_1 = __importDefault(require("./leadingZero"));
/**
 * Convert a number to a Hexadecimal representation (eg. 180 => b4)
 *
 * @example
 * ```ts
 * numberToHex(180); // -> 'b4'
 * ```
 *
 * @param num - Number to convert
 * @return - Hex representation of the given number
 */
function numberToHex(num) {
    return (0, leadingZero_1.default)(Math.round(num).toString(16), 2);
}
exports.default = numberToHex;
